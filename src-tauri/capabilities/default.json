{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Default capability for the quiz control and overlay windows",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
